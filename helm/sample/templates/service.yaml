apiVersion: v1
kind: Service
metadata:
  name: {{ include "readvalue.fullname" . }}
  labels:
    {{- include "readvalue.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "readvalue.selectorLabels" . | nindent 4 }}
