apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "readvalue.fullname" . }}-test-connection"
  labels:
    {{- include "readvalue.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "readvalue.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
